# ansible/inventory.ini

[pypaas_host]
# Placeholder: The IP would be inserted here by a real Terraform run
# For this exercise, assume the IP is known for documentation.
pypaas_server ansible_host=<TERRAFORM_OUTPUT_IP> ansible_user=ubuntu
